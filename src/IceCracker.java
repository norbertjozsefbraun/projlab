public class IceCracker extends Head {
    @Override
    public void clean(Field f) {
        // Skeleton.call("icecracker:IceCracker", "clean(field)");

        // Surface s = f.getSurface();
        // s.breakIce();

        // Skeleton.returnValue("icecracker:IceCracker", "clean");
    }
}